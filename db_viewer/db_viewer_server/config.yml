server:
  port: 8080
  cors_origin: "http://localhost:3000"

# data_store is the single server-side Oracle connection used to store app data (DB_VIEWER_APP_DATA).
# It is not exposed to the web UI. All client connections are managed via the web UI.
data_store:
  user: "LEARN1"
  password: "Welcome"
  host: "localhost"
  port: 1521
  service: "XE"
  schema: "LEARN1"
